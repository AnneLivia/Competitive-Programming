#include <iostream>

using namespace std;
// Finish
int main()
{
    int t, qp, p;
    string name;

    while(true) {
        cin >> t >> qp;
        if(t == 0)
            break;
        while(t--) {
            cin >> name >> p;
            if(p == 1)
                cony+
        }
    }
    return 0;
}
